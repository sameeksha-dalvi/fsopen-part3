{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "license": "MIT",
  "author": "Sameeksha",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../../fsopen-part2/phonebook && npm run build && cp -r dist ../../fsopen-part3/phonebook-backend",
    "deploy:full": "npm run build:ui && cd ../../fsopen-part3/phonebook-backend && git add . && git commit -m 'copy-ui' && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "morgan": "^1.10.1"
  }
}
